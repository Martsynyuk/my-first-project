



+	- (view.php) public function render($view) - переписати щоб шляхи не були захардкоджені.
+	- (autoload_classes.php) function autoload($classname) - тут виходить що перебарається варінати по черзі і які найшло такі загрузило.... а можна то зробити іншим варіантом? (щоб завантажувало тільки те що потрібно)
+	- (constant.php) тут мають бути зібрані усі константи... 
+	- стилізація коду!!!!
+	-header('Location: index'); - заміни 'index' на '/'

	- controllrs:
	$select_user_contacts = new Information(); - в кожному методі це повторюється... думаю то можна забрати. (по дефолту підвантажувати яку треба але передбачити можливість підключатись до іншої моделі)
	$_GET ['id'], $_SESSION ['id'], $_POST['id'] - методи мають працювати з аргументами а не діставати з різних місць дані. (в діспачері дописати функціонал який моніторить змінні в $_POST, $_Get і передає до методів як аргументи "/contacts/editcontact?id=80" => "/contacts/editcontact/80")
	"$view = new View(); $view->set('contactUser', $contactUser); $view->render('contacts_viewcontact');" - це теж можна оптимізувати... більшість методів закінчується view.
+	contactsDefender() - о то функція яка має бути в parent класі
+	return ($contacts); - що то таке?
+	echo 'User already exists '; - в контроллері нічого не повинно виводитись.

	- models:
+	поправити докуметовані коментарі до методів.
+	$mysqli = $this->conect('localhost', 'user', 'root', ''); - ці параметри мають бути теж винесені в constant.php

	- views:
+	де ділись тринарні оператори та скорочений запис?
+	назви вюшок доведи до пуття.
+	$res['0']['Country'] - нащо "0" ?
+	(contacts_selectcontact.html) row 32, 33 не можу прочитати...
